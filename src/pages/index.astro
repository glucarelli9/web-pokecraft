---
import Layout from '../layouts/Layout.astro';
---

<Layout title="Inicio">

  <!-- HERO -->
  <section class="hero">
    <div class="hero-bg"></div>
    <div class="hero-content">
      <div class="badge">🌍 Servidor Argentino</div>
      <h1 class="hero-title">
        <span class="pixel-text">PokeCraft</span>
        <span class="hero-sub">Argentina</span>
      </h1>
      <p class="hero-desc">
        El único servidor donde capturas Pokémon mientras construís en Minecraft.<br />
        <strong>Fabric · Cobblemon · Cobbleverse</strong>
      </p>
      <div class="hero-actions">
        <a href="#como-entrar" class="btn btn-primary">¿Cómo entrar?</a>
        <a href="https://discord.gg/HtheWa3T" target="_blank" rel="noopener" class="btn btn-discord">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
            <path d="M20.317 4.37a19.791 19.791 0 0 0-4.885-1.515.074.074 0 0 0-.079.037c-.21.375-.444.864-.608 1.25a18.27 18.27 0 0 0-5.487 0 12.64 12.64 0 0 0-.617-1.25.077.077 0 0 0-.079-.037A19.736 19.736 0 0 0 3.677 4.37a.07.07 0 0 0-.032.027C.533 9.046-.32 13.58.099 18.057.1 18.099.12 18.14.148 18.17a19.9 19.9 0 0 0 5.993 3.03.078.078 0 0 0 .084-.028 14.09 14.09 0 0 0 1.226-1.994.076.076 0 0 0-.041-.106 13.107 13.107 0 0 1-1.872-.892.077.077 0 0 1-.008-.128 10.2 10.2 0 0 0 .372-.292.074.074 0 0 1 .077-.01c3.928 1.793 8.18 1.793 12.062 0a.074.074 0 0 1 .078.01c.12.098.246.198.373.292a.077.077 0 0 1-.006.127 12.299 12.299 0 0 1-1.873.892.077.077 0 0 0-.041.107c.36.698.772 1.362 1.225 1.993a.076.076 0 0 0 .084.028 19.839 19.839 0 0 0 6.002-3.03.077.077 0 0 0 .032-.054c.5-5.177-.838-9.674-3.549-13.66a.061.061 0 0 0-.031-.03z"/>
          </svg>
          Unirse al Discord
        </a>
      </div>
    </div>
    <div class="hero-scroll">↓</div>
  </section>

  <!-- QUE ES -->
  <section class="section" id="que-es">
    <div class="container">
      <h2 class="section-title">¿Qué es Pokecraft Argentina?</h2>
      <p class="section-subtitle">
        Un servidor de Minecraft con Fabric donde instalamos <strong>Cobblemon</strong> y el pack <strong>Cobbleverse</strong>
        con addons extra, creando una experiencia única de Pokémon dentro de Minecraft.
      </p>
      <div class="cards">
        <div class="card">
          <div class="card-icon">🎮</div>
          <h3>Minecraft + Pokémon</h3>
          <p>Explorá el mundo de Minecraft mientras capturás, entrenás y batallás con más de 400 Pokémon disponibles.</p>
        </div>
        <div class="card">
          <div class="card-icon">🧩</div>
          <h3>Cobbleverse Pack</h3>
          <p>Un modpack curado con Cobblemon como núcleo, más addons que expanden la experiencia con nuevos biomas, items y mecánicas.</p>
        </div>
        <div class="card">
          <div class="card-icon">🇦🇷</div>
          <h3>Comunidad Argentina</h3>
          <p>Servidor local para la comunidad argentina. Baja latencia, staff activo y eventos regulares para toda la comunidad.</p>
        </div>
        <div class="card">
          <div class="card-icon">⚔️</div>
          <h3>PvP y Cooperativo</h3>
          <p>Batallá contra otros entrenadores, formá alianzas, comerciá Pokémon y conquistá el mundo juntos.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- COMO ENTRAR -->
  <section class="section section-dark" id="como-entrar">
    <div class="container">
      <h2 class="section-title">¿Cómo entrar al servidor?</h2>
      <div class="steps">
        <div class="step">
          <div class="step-number">01</div>
          <div class="step-content">
            <h3>Instalá Fabric</h3>
            <p>Descargá e instalá el <strong>Minecraft Launcher</strong> con Fabric para la versión <strong>1.21.1</strong>.
            Podés usar el launcher oficial o alternativas como Prism Launcher.</p>
          </div>
        </div>
        <div class="step">
          <div class="step-number">02</div>
          <div class="step-content">
            <h3>Descargá el Modpack</h3>
            <p>Descargá los mods directamente desde <a href="https://www.mediafire.com/folder/mbefld92q9fxk/Mods_%2B_Sharders" target="_blank" rel="noopener" class="link-green">este link de MediaFire</a> y colocá los archivos en tu carpeta <code>.minecraft/mods</code>. También podés pedir ayuda en el Discord.</p>
          </div>
        </div>
        <div class="step">
          <div class="step-number">03</div>
          <div class="step-content">
            <h3>Conectate al Servidor</h3>
            <p>Abrí Minecraft, andá a <strong>Multijugador → Agregar servidor</strong> y escribí la IP de abajo.
            ¡Ya estás listo para jugar!</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- IP DEL SERVIDOR -->
  <section class="section" id="ip">
    <div class="container">
      <h2 class="section-title">IP del Servidor</h2>
      <p class="section-subtitle">Copiá la IP y pegala en Minecraft → Multijugador → Agregar Servidor</p>
      <div class="ip-box">
        <span class="ip-label">Dirección del servidor</span>
        <div class="ip-display">
          <code id="server-ip">play.pokecraftar.com.ar</code>
          <button class="copy-btn" onclick="copyIP()" title="Copiar IP">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <rect x="9" y="9" width="13" height="13" rx="2" ry="2"></rect>
              <path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"></path>
            </svg>
            <span id="copy-text">Copiar</span>
          </button>
        </div>
        <div class="ip-meta">
          <span>🟢 Versión: Fabric 1.21.1</span>
          <span>🧩 Modpack: Cobbleverse</span>
        </div>
      </div>
    </div>
  </section>

  <!-- DISCORD CTA -->
  <section class="section discord-section">
    <div class="container">
      <div class="discord-card">
        <div class="discord-icon">
          <svg width="48" height="48" viewBox="0 0 24 24" fill="currentColor">
            <path d="M20.317 4.37a19.791 19.791 0 0 0-4.885-1.515.074.074 0 0 0-.079.037c-.21.375-.444.864-.608 1.25a18.27 18.27 0 0 0-5.487 0 12.64 12.64 0 0 0-.617-1.25.077.077 0 0 0-.079-.037A19.736 19.736 0 0 0 3.677 4.37a.07.07 0 0 0-.032.027C.533 9.046-.32 13.58.099 18.057.1 18.099.12 18.14.148 18.17a19.9 19.9 0 0 0 5.993 3.03.078.078 0 0 0 .084-.028 14.09 14.09 0 0 0 1.226-1.994.076.076 0 0 0-.041-.106 13.107 13.107 0 0 1-1.872-.892.077.077 0 0 1-.008-.128 10.2 10.2 0 0 0 .372-.292.074.074 0 0 1 .077-.01c3.928 1.793 8.18 1.793 12.062 0a.074.074 0 0 1 .078.01c.12.098.246.198.373.292a.077.077 0 0 1-.006.127 12.299 12.299 0 0 1-1.873.892.077.077 0 0 0-.041.107c.36.698.772 1.362 1.225 1.993a.076.076 0 0 0 .084.028 19.839 19.839 0 0 0 6.002-3.03.077.077 0 0 0 .032-.054c.5-5.177-.838-9.674-3.549-13.66a.061.061 0 0 0-.031-.03z"/>
          </svg>
        </div>
        <h2>Unite a nuestra comunidad</h2>
        <p>En el Discord encontrás el modpack, ayuda de la comunidad, anuncios de eventos y mucho más.</p>
        <a href="https://discord.gg/HtheWa3T" target="_blank" rel="noopener" class="btn btn-discord btn-large">
          Unirse al Discord
        </a>
      </div>
    </div>
  </section>

</Layout>

<script>
function copyIP() {
  navigator.clipboard.writeText('play.pokecraftar.com.ar').then(() => {
    const btn = document.getElementById('copy-text');
    if (btn) {
      btn.textContent = '¡Copiado!';
      setTimeout(() => { btn.textContent = 'Copiar'; }, 2000);
    }
  });
}
(window as any).copyIP = copyIP;
</script>

<style>
  /* HERO */
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: 6rem 1.5rem 4rem;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at 20% 50%, rgba(74, 222, 128, 0.08) 0%, transparent 60%),
      radial-gradient(ellipse at 80% 20%, rgba(248, 113, 113, 0.08) 0%, transparent 50%),
      radial-gradient(ellipse at 60% 80%, rgba(167, 139, 250, 0.06) 0%, transparent 50%);
    pointer-events: none;
  }

  .hero-content {
    position: relative;
    max-width: 800px;
  }

  .badge {
    display: inline-block;
    background: rgba(74, 222, 128, 0.12);
    border: 1px solid rgba(74, 222, 128, 0.3);
    color: var(--green);
    font-size: 0.8rem;
    font-weight: 600;
    padding: 0.35rem 1rem;
    border-radius: 999px;
    margin-bottom: 1.5rem;
    letter-spacing: 0.05em;
  }

  .hero-title {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 1.5rem;
  }

  .pixel-text {
    font-family: 'Press Start 2P', monospace;
    font-size: clamp(2rem, 7vw, 4.5rem);
    background: linear-gradient(135deg, var(--green), #22d3ee, var(--yellow));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1.2;
  }

  .hero-sub {
    font-family: 'Press Start 2P', monospace;
    font-size: clamp(0.7rem, 2.5vw, 1.2rem);
    color: var(--text-muted);
    letter-spacing: 0.1em;
  }

  .hero-desc {
    font-size: 1.15rem;
    color: var(--text-muted);
    margin-bottom: 2.5rem;
    line-height: 1.8;
  }

  .hero-desc strong {
    color: var(--text);
  }

  .hero-actions {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .hero-scroll {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    color: var(--text-muted);
    font-size: 1.2rem;
    animation: bounce 2s infinite;
  }

  @keyframes bounce {
    0%, 100% { transform: translateX(-50%) translateY(0); }
    50% { transform: translateX(-50%) translateY(8px); }
  }

  /* BOTONES */
  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.75rem 1.75rem;
    border-radius: 8px;
    font-size: 0.95rem;
    font-weight: 600;
    transition: all 0.2s;
    border: none;
    cursor: pointer;
  }

  .btn-primary {
    background: var(--green);
    color: #000;
  }

  .btn-primary:hover {
    background: #22c55e;
    transform: translateY(-1px);
  }

  .btn-discord {
    background: var(--discord);
    color: white;
  }

  .btn-discord:hover {
    background: #4752c4;
    transform: translateY(-1px);
  }

  .btn-large {
    padding: 1rem 2.5rem;
    font-size: 1.05rem;
  }

  /* SECCIONES */
  .section {
    padding: 5rem 1.5rem;
  }

  .section-dark {
    background: var(--surface);
  }

  .container {
    max-width: 1100px;
    margin: 0 auto;
  }

  .section-title {
    font-family: 'Press Start 2P', monospace;
    font-size: clamp(0.9rem, 2.5vw, 1.3rem);
    text-align: center;
    margin-bottom: 1rem;
    color: var(--text);
    line-height: 1.5;
  }

  .section-subtitle {
    text-align: center;
    color: var(--text-muted);
    max-width: 650px;
    margin: 0 auto 3rem;
    font-size: 1rem;
    line-height: 1.8;
  }

  .section-subtitle strong {
    color: var(--text);
  }

  /* CARDS */
  .cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
    gap: 1.5rem;
  }

  .card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: 12px;
    padding: 2rem 1.5rem;
    transition: border-color 0.2s, transform 0.2s;
  }

  .card:hover {
    border-color: var(--green);
    transform: translateY(-3px);
  }

  .card-icon {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  .card h3 {
    font-size: 1rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
    color: var(--text);
  }

  .card p {
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.7;
  }

  /* STEPS */
  .steps {
    display: flex;
    flex-direction: column;
    gap: 2rem;
    max-width: 700px;
    margin: 0 auto;
  }

  .step {
    display: flex;
    gap: 1.5rem;
    align-items: flex-start;
  }

  .step-number {
    font-family: 'Press Start 2P', monospace;
    font-size: 0.7rem;
    color: var(--green);
    background: rgba(74, 222, 128, 0.1);
    border: 1px solid rgba(74, 222, 128, 0.25);
    border-radius: 8px;
    padding: 0.75rem 1rem;
    min-width: 60px;
    text-align: center;
    flex-shrink: 0;
  }

  .step-content h3 {
    font-size: 1.05rem;
    font-weight: 600;
    margin-bottom: 0.4rem;
  }

  .step-content p {
    font-size: 0.9rem;
    color: var(--text-muted);
    line-height: 1.7;
  }

  .step-content strong {
    color: var(--green);
  }

  .link-green {
    color: var(--green);
    text-decoration: underline;
    text-underline-offset: 3px;
  }

  .step-content code {
    font-family: monospace;
    background: var(--surface2);
    border: 1px solid var(--border);
    padding: 0.1rem 0.4rem;
    border-radius: 4px;
    font-size: 0.85em;
    color: var(--yellow);
  }

  /* IP BOX */
  .ip-box {
    max-width: 600px;
    margin: 0 auto;
    background: var(--surface);
    border: 2px solid var(--green);
    border-radius: 16px;
    padding: 2.5rem;
    text-align: center;
    box-shadow: 0 0 40px rgba(74, 222, 128, 0.08);
  }

  .ip-label {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--text-muted);
    display: block;
    margin-bottom: 1rem;
  }

  .ip-display {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    background: var(--surface2);
    border: 1px solid var(--border);
    border-radius: 10px;
    padding: 1rem 1.5rem;
    margin-bottom: 1.25rem;
  }

  .ip-display code {
    font-family: 'Press Start 2P', monospace;
    font-size: clamp(0.6rem, 2.5vw, 0.95rem);
    color: var(--green);
    letter-spacing: 0.05em;
  }

  .copy-btn {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    background: rgba(74, 222, 128, 0.1);
    border: 1px solid rgba(74, 222, 128, 0.3);
    color: var(--green);
    border-radius: 6px;
    padding: 0.4rem 0.75rem;
    font-size: 0.8rem;
    cursor: pointer;
    transition: all 0.2s;
    white-space: nowrap;
    font-family: 'Inter', sans-serif;
  }

  .copy-btn:hover {
    background: rgba(74, 222, 128, 0.2);
  }

  .ip-meta {
    display: flex;
    gap: 1.5rem;
    justify-content: center;
    flex-wrap: wrap;
    font-size: 0.85rem;
    color: var(--text-muted);
  }

  /* DISCORD */
  .discord-section {
    background: linear-gradient(135deg, rgba(88, 101, 242, 0.08) 0%, transparent 60%);
  }

  .discord-card {
    max-width: 600px;
    margin: 0 auto;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .discord-icon {
    color: var(--discord);
    background: rgba(88, 101, 242, 0.1);
    border-radius: 50%;
    width: 80px;
    height: 80px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .discord-card h2 {
    font-size: 1.6rem;
    font-weight: 700;
  }

  .discord-card p {
    color: var(--text-muted);
    font-size: 1rem;
    line-height: 1.7;
    max-width: 450px;
  }

  @media (max-width: 600px) {
    .step {
      flex-direction: column;
    }
    .step-number {
      min-width: auto;
    }
    .ip-display {
      flex-direction: column;
    }
  }
</style>
